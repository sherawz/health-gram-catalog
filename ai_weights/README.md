# AI Weights

This directory handles the metadata processing for AI Weights within the Health Gram Catalog.

**Goal**: Fetch, parse, and process metadata for AI Weights and store it into an SQLite database.
**Collaboration**: The AI Weights team will work within this directory to write their specific `CommandLineTool` definitions and scripts.
