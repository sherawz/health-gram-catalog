# Health Gram Explorer

This directory is for the JavaScript metadata browser for the Health Gram Catalog.

**Goal**: Build a web application to explore the combined SQLite database.
**Collaboration**: The Explorer team will work here. Ensure the application supports partial reads (HTTP VFS) so large databases don't have to be loaded into memory.
