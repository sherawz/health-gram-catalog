# Cohorts

This directory handles the metadata processing for Well-Consented Human Cohorts within the Health Gram Catalog.

**Goal**: Fetch, parse, and process metadata for human cohorts and store it into an SQLite database.
**Collaboration**: The Cohorts team will work within this directory to write their specific `CommandLineTool` definitions and scripts.
